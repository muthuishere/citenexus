# Tasks

## 1. The resolution rule (`lang/fallback.py`)

- [x] 1.1 Red: explicit override beats a reliable detection
- [x] 1.2 Red: reliable detection used when nothing is explicit
- [x] 1.3 Red: conversation language is rung 3
- [x] 1.4 Red: `languages_in_evidence` is ignored in every combination
- [x] 1.5 Red: unspecified + no detection → configured default
- [x] 1.6 Red: the ingest call shape (`detection=` + `default_answer_language=`
      only) still resolves detection → default
- [x] 1.7 Implement the four-rung chain; keep `languages_in_evidence` accepted
      and ignored
- [x] 1.8 Add the `"auto"` sentinel constant + `resolve_requested_answer_language`
      (question + detector → language), with tests for detector present /
      unreliable / absent

## 2. Config knob (`config/schema.py`)

- [x] 2.1 Red: `multilingual.default_answer_language` defaults to `"en"`
- [x] 2.2 Red: a set `fallback_language` still wins (deprecated alias)
- [x] 2.3 Implement the field + the resolved-default property; point
      `from_config` at it

## 3. Flows carry the detector (`answer/flow.py`, `answer/agentic.py`, `client.py`)

- [x] 3.1 Red: `ask(q)` over a Telugu-dominant pool stamps the default, not `te`
- [x] 3.2 Red: `ask(q, answer_language="auto")` with a detector stamps the
      question's language
- [x] 3.3 Red: `ask(q, answer_language="fr")` stamps `fr` whatever the pool
- [x] 3.4 Red: the deep strategy agrees with the strict strategy on all three
- [x] 3.5 Implement: `AnswerFlow`/`AgenticAnswerFlow` take `detector=`; client
      stores and injects it; the client stops normalising `"auto"` to `None`

## 4. Refusal attribution (`answer/flow.py`)

- [x] 4.1 Red: English question + English irrelevant pool → evidence-absent reason
- [x] 4.2 Red: same + one unreadable candidate → still the evidence-absent reason,
      and `unsupported_scripts` still reports the script
- [x] 4.3 Red: all candidates unreadable → "no readable evidence found;
      unsupported script: …"
- [x] 4.4 Red: unreadable question → unchanged capability reason
- [x] 4.5 Red: gate failure with an unreadable candidate present → the gate reason
- [x] 4.6 Implement: partition the pool into readable / script-excluded and blame
      only the excluded

## 5. Unreachable-authority signal (`answer/flow.py`)

- [x] 5.1 Red: answered Result with an unreadable sibling document reports the
      script AND names the document in `missing_evidence`
- [x] 5.2 Red: the unreadable passage is never cited
- [x] 5.3 Red: a fully readable corpus produces empty signal + empty
      `missing_evidence`
- [x] 5.4 Red: the measured leave-carry-forward shape end-to-end (English handbook
      answers, unreadable annexure present, caller can detect it)
- [x] 5.5 Implement

## 6. Gates

- [x] 6.1 `cd python && task check` green EXCEPT the one expected, deliberate
      failure: `test_committed_fixtures_match_regeneration` reports
      `conformance/cases/language.json` stale. That is this change's BREAKING
      verdict shift and is regenerated by the conformance owner, not here.
- [x] 6.2 `uv run python ../spikes/library-stress/stress.py` — four probes pass
- [x] 6.3 Report the exact `conformance/cases/language.json` regeneration to the
      conformance owner (see `design.md`); DO NOT regenerate it here
