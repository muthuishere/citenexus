# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# uv
# (uv.lock IS committed)

# Vendored assets (downloaded on first use)
assets/models/

# Env / secrets — never commit (but keep the committed template)
*.env
.env
.env.*
!.env.example
vault/
infra/vault/

# Local data / scratch
.DS_Store
*.lance/
example/.data/

# Example runtime data (LocalFs store created by `task local:example`)
example/.citenexus-data/
.citenexus-data/

# Rust core build artifacts
rust/target/

# agent worktrees
.claude/worktrees/
example/.trustrag-data/
.trustrag-data/

# Language ports
js/node_modules/
# any nested node_modules (spikes, examples, tooling)
node_modules/
js/dist/
golang/**/*.test

# Built brain binary (CLI lives in its own repo)
/brain

# personal corpus (bank/card statements, private docs) — never commit
corpus/
# ...but the examples ship AUTHORED, public test corpora. Without this negation the
# bare rule above silently swallowed examples/multilingual/corpus/ on commit, so CI
# had the tests and none of the documents. examples/law-authority/corpus/ only
# survived because it was tracked before the rule existed.
!examples/*/corpus/
!examples/*/corpus/**
